{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "buildJar",
      "type": "shell",
      "command": "./gradlew build",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}
